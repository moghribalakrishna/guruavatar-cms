'use strict';

/**
 * mindfulness-community-signup router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::mindfulness-community-signup.mindfulness-community-signup');
