'use strict';

/**
 * mindfulness-community-signup service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::mindfulness-community-signup.mindfulness-community-signup');
